const assert = require('assert');

describe('User Authentication Service Tests', () => {

  describe('#generateRandomPassword()',  () => {
    it('password len should be 32',  () => {
      assert.equal(32, 32);
    });
  });

  describe('#registerUser()',  () => {
    it('Valid registration',  () => {
      assert.equal(-1, -1);
    });
  });



});